import type { EntityType } from '../types/graph';
export const entities: any[] = [];
export const getEntitiesByType = (type: EntityType) => [];
export const getEntitiesByCase = (caseId: string) => [];
