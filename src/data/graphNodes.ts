import type { GraphNode } from '../types/graph';
export const graphNodes: GraphNode[] = [];
export const getGraphNodesByCase = (caseId: string) => [];
export const getGraphNodeById = (id: string) => undefined;
